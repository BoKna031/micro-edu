spring:
  application:
    name: guest-service
  datasource:
    url: jdbc:mysql://localhost:3308/profile-service-db
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

server:
  port:8083
